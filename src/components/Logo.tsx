import React from 'react';

const Logo: React.FC = () => {
    return (
        <div className="logo">
            <img src="/path/to/logo.png" alt="Application Logo" />
        </div>
    );
};

export default Logo;